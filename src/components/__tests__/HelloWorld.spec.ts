import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import SiteTitle from '../SiteTitle.vue'

// describe('SiteTitle', () => {
//   it('renders properly', () => {
//     const wrapper = mount(SiteTitle, { props: { msg: 'Hello Vitest' } })
//     expect(wrapper.text()).toContain('Hello Vitest')
//   })
// })
